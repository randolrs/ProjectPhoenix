<% if user_signed_in? %>

<div class="row">
	<div class = "span6 offset3">

		<div class = "well">

		 <%= @gig.desription %>

		<p>
		  <%= @gig.user.name %>
		</p>

		<p>
		  <%= @gig.per_hour %>
		</p>


		<% if current_user == @gig.user %>
		<%= link_to 'Edit', edit_gig_path(@gig) %>
		<%= link_to 'Edit', bid_path(@gig) %>
		<% end %>
		

		<%= link_to 'Back', gigs_path %>

		</div>
	</div>
</div>

	<% elsif musician_signed_in? %>
        <td><%= @gig.desription %></td>
        
    <% end %>


