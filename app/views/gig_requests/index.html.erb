<h1>Listing gig_requests</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gig_requests.each do |gig_request| %>
  <tr>
    <td><%= gig_request.description %></td>
    <td><%= link_to 'Show', gig_request %></td>
    <td><%= link_to 'Edit', edit_gig_request_path(gig_request) %></td>
    <td><%= link_to 'Destroy', gig_request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gig request', new_gig_request_path %>
